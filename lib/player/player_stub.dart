import 'package:flutter/material.dart';

Widget buildPlayer(BuildContext context, String embedUrl) {
  return const Center(
    child: Text(
      'Player not implemented on this platform',
      style: TextStyle(color: Colors.white),
    ),
  );
}
